package application;

public interface Collider {
	/* The Collider interface will set out collision detection & resolution methods
	 * TODO isProximal()
	 * TODO detectCollision(Collider other)
	 * TODO resolveCollision(Collider other) */
}
